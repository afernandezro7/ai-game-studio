/**
 * TravelService — Calculates travel time between map coordinates.
 *
 * Formula: distance = sqrt((x2-x1)² + (y2-y1)²)
 * travelTime = distance / slowestTroopSpeed (fields/hour)
 */

export class TravelService {
  // TODO: implement travel time calculation
}
